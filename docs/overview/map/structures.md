# Structures

## Overview

Structure tiles are any tiles on the map that are not terrain tiles. Structures are groups of structure tiles that form something recognizable, like a [tree] or a shelter.

## Properties

- Can be placed/removed from the tile map (static world).
- Can be walked passed (i.e., no colliders).

[tree]: into-the-woods/map/structure/tree
